# Contributing

1. Fork the repo and create a branch off `main`.
2. `make build && make up` to run the stack locally.
3. Write a test for your change where practical — `make test` must pass.
4. `make lint` must pass (ruff).
5. Open a PR with a clear description of what changed and why.

Please don't commit `.env` — it's gitignored on purpose.
